<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Form View -->
    <record id="oh_appraisal_industry_view_form" model="ir.ui.view">
        <field name="name">oh.appraisal.industry.form</field>
        <field name="model">oh.appraisal.industry</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <button name="toggle_active" type="object" class="oe_stat_button" icon="fa-archive">
                            <field name="active" widget="boolean_button" options="{'terminology': 'archive'}"/>
                        </button>
                    </div>
                    <group>
                        <field name="name"/>
                        <field name="code"/>
                        <field name="sequence"/>
                    </group>
                    <group>
                        <field name="description"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <!-- List View -->
    <record id="oh_appraisal_industry_view_list" model="ir.ui.view">
        <field name="name">oh.appraisal.industry.list</field>
        <field name="model">oh.appraisal.industry</field>
        <field name="arch" type="xml">
            <list>
                <field name="sequence" widget="handle"/>
                <field name="name"/>
                <field name="code"/>
                <field name="active" invisible="1"/>
            </list>
        </field>
    </record>

    <!-- Action -->
    <record id="action_oh_appraisal_industry" model="ir.actions.act_window">
        <field name="name">Industry Types</field>
        <field name="res_model">oh.appraisal.industry</field>
        <field name="view_mode">list,form</field>
    </record>
</odoo>